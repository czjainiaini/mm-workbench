# 维护与风险记录

更新时间：2026-08-12

扫描基线：`94ca76b8fefdb5072391c69f1365bf5ea0a70887`

范围：仓库跟踪文件、历史提交、启动链路、工作台内联脚本、发布模块与本地 HTTP 服务。

## 当前结论

- 项目是零第三方运行时依赖的 Node.js 本地工作台；本次未新增依赖。
- 常见密钥模式历史扫描未发现已提交的 API Key 或 Token。
- 本次修复保持默认角色 `204786`、美化包格式、站点 API 契约和沙盒注入流程不变。
- 未执行真实模型请求、站点登录、角色卡发布或线上部署。

## 风险登记

| 编号 | 优先级 | 状态 | 风险与处理 |
| --- | --- | --- | --- |
| R-001 | P0 | 已缓解 | API Key 已从 `localStorage`、端点档案和模型请求体迁出，由本地服务写入静态路径不可访问的私有仓库；接口只返回是否配置，旧 Key 首次启动自动迁移并清除。工作台与真站预览仍同源，预览脚本理论上仍可控制父页或滥用已配置的模型能力；彻底隔离需要跨 origin RPC 重构，不能在保持全部同步 DOM 编辑能力的前提下作为小修暗改。 |
| R-002 | P1 | 已修复 | AI 全自动模式原先可直接执行模型生成的 JS，修正轮也能引入高风险动作。现在 `run_js/js/load_package/publish/clear_css` 都必须人工确认，修正轮遇到高风险动作会停止。 |
| R-003 | P1 | 已修复 | 聊天、创作和发布目标写死为角色 `204786`。设备栏现可配置角色 ID，默认值不变；非法 ID 在请求前拒绝。 |
| R-004 | P1 | 已缓解 | 本地服务原先监听所有网卡并发送通配 CORS；LLM 中继接受任意目标。现在仅监听 `127.0.0.1`、移除通配 CORS、仅接受 POST/HTTPS、限制 1 MiB 请求，并在连接前拒绝本机、私网和保留地址且固定已校验 DNS 地址。 |
| R-005 | P1 | 已修复 | 模型服务返回的模型 ID 被拼进 `innerHTML`，可形成 DOM 注入。现在全部使用 `textContent` 和 DOM API 渲染。 |
| R-006 | P2 | 已修复 | bridge 触发限流时调用未定义的 `reportError`。现改为已定义的 `window.__wb_reportError`。 |
| R-007 | P2 | 已修复 | 发布前读取规则与目标卡片字段作为写前日志；所有规则写入和卡片保存都先登记尝试项。任一步失败会恢复被更新规则、删除本次新增规则、恢复本次触及的卡片字段，并报告补偿是否完整；故障注入覆盖规则中途失败和卡片保存失败。 |
| R-008 | P2 | 部分缓解 | 零依赖离线冒烟测试与 GitHub Actions 现覆盖语法、标记唯一性、静态资源、路径隔离、密钥仓库、LLM 中继边界、发布补偿回滚和注册表可复现性；`workbench.html` 仍是大单文件，后续应在测试保护下分模块。 |
| R-009 | P2 | 已修复 | 新增可追踪的 `src/style-registry.base.json` 作为 64 条基础规范；生成器只依赖该规范与 `src/live-patch.json`，可确定性重建 97 条运行注册表，`--check` 已纳入统一自检。私有参考包不再是构建依赖。 |
| R-010 | P3 | 待确认 | `启动工作台.bat` 与 `启动工作台.bat.txt` 内容相同。可能是下载平台兼容副本；未确认发布用途前不删除。 |
| R-011 | P2 | 已修复 | 已移除“本地 Ollama”误导示例，并在端点档案帮助中明确只接受公网 HTTPS、拒绝本机与私网地址；未放宽 SSRF 防护。 |
| R-012 | P2 | 已缓解 | AI 可见会话原本会持久化，但真正发给模型的 `llmHistory` 只在内存中保留最近 10 条，刷新后上下文丢失且无法控制预算。现按会话持久化模型轮次与摘要，支持上下文/输出预算、手动压缩和接近上限时自动总结；旧数据保持兼容。 |
| R-013 | P2 | 已缓解 | 项目状态原先散落在多个 localStorage 键中，没有版本恢复或交付检查。现增加白名单化 v1 快照（不含密钥、模型档案、聊天、角色 ID 和登录态）、恢复前自动备份、导入大小/结构校验与最多 30 份保留。 |
| R-014 | P2 | 已缓解 | 导出/发布原先可绕过统一验证。现导出自动做包结构检查，发布强制验证真站聊天模式、实时桥和真实消息列表；Test Mode 同时展示包能力与相对最近快照的差异。 |

## 体验维护记录（2026-08-12）

- 首次体验改为真站优先的四步引导：连接真实聊天页、即时换色、撤销练习、进入项目闭环；登录/人机验证明确引导到新页完成。
- 沙盒空状态和模式下拉把真站聊天页标为推荐；本地复刻明确标注为功能不完整的离线演示。对旧版自动停留在本地模式的用户执行一次性真站迁移，之后仍尊重手动选择。
- 增加 6 套工作台界面主题；主题偏好本地保存，且不污染沙盒页面配色。
- 页面配色、组件、AI 样式与快捷修改统一接入 30 步撤销/重做和临时前后对比。
- AI 结果改为结果卡：默认展示完成情况，选择器缺失等信息收进技术详情；只有可回滚动作才显示撤销按钮。
- 控制台默认折叠、错误时自动展开；体检增加图片资源加载结果；SVG 静态资源返回正确 MIME。
- 标签页补齐键盘方向键切换、ARIA 关联与焦点状态；窄屏默认折叠两侧栏，仍可手动展开。
- 新增项目工作室：4 套模板、白名单项目快照、导入/导出、恢复点、变更历史、能力提示、快照差异与发布前 Test Mode。
- AI 设置新增 2K–128K 上下文预算、256–8192 输出上限、最近轮次和自动总结开关；模型记忆绑定各自会话并跨刷新恢复。

## 同类项目对比与借鉴边界

| 项目 | 许可证/状态 | 可借鉴项 | 适配建议 |
| --- | --- | --- | --- |
| [Stylebot](https://github.com/ankit/stylebot) | MIT；成熟浏览器扩展 | 点选元素、即时保存、CSS 编辑器、内容脚本隔离 | 可参考控制面与页面注入分层；允许复用 MIT 思路或代码，但引入时仍保留版权说明。 |
| [Stylus](https://github.com/openstyles/stylus) | GPL-3.0；成熟且持续维护 | CSS lint、JSON 备份、样式版本/更新、轻量内容脚本 | 本项目为 MIT，不直接复制 GPL 代码；只借鉴产品与架构概念，自行实现。 |
| [GrapesJS](https://github.com/GrapesJS/grapesjs) | BSD-3-Clause；持续发布 | command bus、可拦截动作、状态化命令、版本化 project data、autosave/dirty count | 不建议现在整体引入大框架；先把现有操作统一成命令契约，再拆分存储层。 |
| [tavern-mmd](https://github.com/yofengi/tavern-mmd) | MIT；小型垂直项目 | 零依赖 JSON 校验、平台差异矩阵、预览构建、交付检查清单 | 优先借鉴验证器与检查清单；不要把未验证的平台实测结论当稳定 API。 |
| [SillyTavern Regex](https://github.com/SillyTavern/SillyTavern-Docs/blob/main/extensions/Regex.md) | 官方文档 | scoped/global 范围、导入导出、实时 Test Mode | 给包增加作用域、启用状态、测试输入/输出和发布前 diff。 |
| [browser-use](https://github.com/browser-use/browser-use) | MIT；活跃 | `allowed_domains`、`prohibited_domains`、IP 地址阻断、敏感数据域约束 | 为 AI 动作和包清单增加域名/能力白名单，默认拒绝网络与发布权限。 |
| [Penpal](https://github.com/Aaronius/penpal) | MIT；持续维护 | 基于 `postMessage` 的跨 origin 请求/响应桥与超时处理 | 未来拆分预览 origin 时借鉴协议边界；本次未复制代码，避免在同步 DOM 编辑链上做未经回归的大改。 |

## 建议路线

1. P0：密钥已移出被代理页面可读的 Web Storage；后续单独立项拆分控制台 origin 与站点预览 origin，建立来源校验、能力白名单和超时/撤销语义完整的异步消息桥。
2. P1：为美化包增加 `schemaVersion` 与 `capabilities`（如 `css`、`dom`、`script`、`network`、`publish`），导入时静态校验并展示权限差异。
3. P1：发布前快照、项目差异、Test Mode、远端写前日志和部分失败补偿回滚已完成；下一步仅在平台提供版本号/事务契约后增加并发修改检测。
4. P2：继续补发布模拟和浏览器自动化；覆盖稳定后再拆分 `workbench.html` 的 AI 会话与组件模块。
5. P2：项目数据已提供白名单 v1 备份/恢复；下一步增加显式 schema migration 和跨浏览器加密备份选项。

## 本次验证

- 统一入口：`node scripts/smoke-test.js`，本轮执行通过；GitHub Actions 使用同一条命令。
- Node 语法检查：全部独立 JS 与 2 个 HTML 内联脚本通过。
- JSON 解析：全部跟踪 JSON 通过；样式注册表无需私有 mock 输入即可确定性重建。
- 本地 HTTP：工作台 200、缺失文件 404、编码路径穿越 403、错误方法 405、坏 JSON 400、超限请求 413。
- 网络边界：监听地址确认为 `127.0.0.1`；恶意 Origin 无通配 CORS；私网 LLM 目标在连接前返回 400。
- 离线单元式检查：密钥迁移与服务端持有边界、危险动作门禁、模型 ID 纯文本渲染、发布规则/卡片两类故障补偿回滚、注册表可复现性均通过。
- Playwright 真浏览器验收：真站聊天页被默认选中且真实消息结构就绪；模板即时注入、自动恢复点、手动快照、Test Mode 通过、AI 上下文设置跨刷新保存；控制台最终 0 error / 0 warning。未调用模型 API、未执行角色卡发布。
