# 魅魔工作台 · 开发守则

## 底层开发思维（总纲，一切开发活动必须遵循）

**坚决杜绝闭门造车、重复造轮子。**

1. 启动项目或制定较大方案前，优先去 GitHub（代码托管平台）查找同类项目、文档、实现思路和源码。
2. 有成熟现成项目时，优先评估 license（许可证）、维护状态、安全风险和适配成本后复用。
3. 简单 bug（问题）修复、明确小改动或离线任务可不强制调研。
4. 多检索、多看、多拆解开源项目，可复用内容直接取用，有参考价值的方案借鉴参考。
5. 多套开源方案交叉对比，取优势、剔除缺陷糟粕。

### 本项目已践行的复用清单

| 需求 | 复用来源 | 说明 |
|---|---|---|
| 差异对比 | Monaco Editor Diff / jsdiff | 修改记录与 diff 展示 |
| 注入载体 | 油猴 / MV3 content script 惯例 | 追加式 `<style>`、不改站点原节点 |
| Agent 浏览器操作 | browser-use 思路 | DOM Snapshot + 元素定位 |
| 可视化布局 | GrapesJS 三栏范式 | 左文件树 / 中画布 / 右属性 |
| 模型接入 | OpenAI 兼容协议 | `/chat/completions` + `/models`，默认腾讯云混元 |
| 正则包格式规范 | [tavern-mmd](https://github.com/yofengi/tavern-mmd)（MIT） | 双格式归一化：MMD 导入 JSON + SillyTavern 正则数组 |

## 架构速览

```
http://localhost:<PORT>
├── /（根路径）          = sexyai.ai 整站反向代理（同源沙盒）
│    └── 每个 HTML 自动注入 wb-bridge.js / loader.js / publisher.js
├── /sw.js              = Service Worker（根作用域，拦截逃逸的绝对地址请求）
├── /wb/api/llm         = 大模型中继（chat/completions + models，避浏览器 CORS）
├── /wb/api/llm-secret  = 本地密钥仓库（只返回配置状态，不回传 Key）
└── /wb/                = 工作台静态文件（workbench.html 等）
```

- **三模式沙盒**：真站聊天页 / 真站创作界面 / 本地复刻（离线），另有"通用网址"模式（sexyai.ai 域名自动走代理同源化，外部域名直嵌）。
- **wb-bridge**：console 捕获上报、实时 CSS/JS 注入；同源父子窗用直接函数投递（`parent.__wb_receive`），避免 postMessage 撞上站点 message 处理器。
- **publisher**：角色卡一键发布（正则规则 upsert/追加 + 卡片字段整卡回写）；写前保存远端状态，任何中途失败按反向顺序补偿回滚。
- **AI Agent 回路**：用户指令 + 沙盒快照（DOM 区域命中/已注入样式）→ LLM（经 /wb/api/llm 中继）→ 结构化动作序列
  （apply_theme/apply_css/run_js/load_package/publish/clear_css）→ 逐条执行 → **自动验证选择器命中** →
  未生效时把结果回喂模型修正（最多 2 轮）；未配 API Key 时降级为内置规则引擎并明确提示。
  配色类需求走 buildThemeCss 全区域生成器（背景/顶栏/双气泡/状态栏/快捷栏/输入区），避免只改到部分元素。
- **对话持久化**：AI 消息流（含执行卡片）按 {ts,type,html/save} 存 localStorage（wb-chat），刷新自动恢复，「🗑」手动清空。
- **密钥所有权**：浏览器 localStorage 只保存端点/模型与 `hasApiKey`；Key 由 `server.js` 原子写入 `.mm-workbench-private/llm-secrets.json`，文件权限尽量收紧为仅当前用户，模型请求体不再携带 Key。

## Pi Agent 借鉴决策（调研：github.com/earendil-works/pi，77K star，MIT 类许可）

| Pi 特性 | 决策 | 落地方式 / 暂缓理由 |
|---|---|---|
| 极简系统提示词 | ✅ 已实施 | LLM_SYSTEM/PLAIN 瘦身至动作协议；类名参考（CLASS_REF）改为首轮随快照按需注入并留在对话历史 |
| 执行过程逐步可见 | ✅ 已实施 | progress 过程卡片：⏳/✅/❌ 逐动作刷新、失败附原因、可持久化恢复 |
| 先计划后执行 | ✅ 已有 | planCard 确认卡 + 自动修正轮（最多 2 轮） |
| 渐进式披露 Skills | ✅ 轻量落地 | skills/技能库.md 能力清单（面向用户）；不引入自动加载机制，避免过度设计 |
| 安全可恢复 | ✅ 已实施 | 会话删除 confirm 确认；清空对话后提供「↩ 撤销」 |
| 会话 fork 分叉 | ⏸ 暂缓 | 需改消息树结构，收益不及成本，待多会话用熟后再评估 |
| pi-ai 统一模型层（流式/多 provider） | ⏸ 暂缓 | 需引入 Node 依赖并重构中继，有破坏风险；现有纯文本兼容+宽容解析已覆盖多数模型 |
| Pi SDK 作 Agent 内核 | ⏸ 暂缓 | 相当于重构 AI 回路，等前两项稳定后再评估 |

## 可视化编辑器选型评估（组件画布）

| 方案 | License | 结论 |
|---|---|---|
| GrapesJS | BSD-3 | 定位是“从零建网页”，与“在现有站点上调样式”场景不符，弃用 |
| litegraph.js | MIT | Canvas 节点引擎，强在数据流连线；我们的组件间无真实连线语义，学习曲线反而违背新手目标 |
| rete.js | MIT | 现代节点框架，若未来真需要连线逻辑再引入 |
| ComfyUI 前端 | AGPL | 许可证污染风险，直接排除 |

**最终决策**：采用轻量版“组件卡片 + 拖拽排序（原生 HTML5 DnD，零依赖）+ 参数面板 + 实时合成预览”。
关键技术点：组件 CSS 选择器统一带 `:not(#mm-z)` 把特异度升到 ID 级，稳赢站点/美化包的纯 class 规则；
前后对比通过临时清空 wb-live-css 实现；组件状态持久化在 localStorage(wb-components)，沙盒就绪后自动重新应用。

## 站点关键契约（实测沉淀）

- 双层 iframe：主应用在同源 `#chatIframe` 内，DOM 操作必须穿透。
- uni-app H5 + Vue 3，rem 缩放公式 `html字号 = 宽/750×32`；正文固定 15px。
- CSP 允许内联 script/style（`unsafe-inline`）。
- 角色卡 API（全部 POST + Cookie）：`/api/role/query {id}`、`/api/role/save {整卡}`、
  `/api/role/regexp/list {roleId}`、`/api/role/regexp/save [Regexp]`（追加语义）、
  `/api/role/regexp/delete {id}`；分类 ID 不连续，报错时动态取 `/api/role/category/list` 重试。
- 风控：429=限流（批量写入间隔 ≥350ms），403=内容审核。
- 正则包双格式（tavern-mmd 规范）：① MMD 导入 JSON（pageDepth/statusbar/beginning/regex_scripts 四字段）；
  ② SillyTavern 正则数组（含 placement/markdownOnly/disabled 等全字段，或卡内嵌 data.extensions.regex_scripts）。
  publisher 自动归一化；平台上限 130 条；disabled 条目自动跳过；换行必须转义为 `\n`、无 BOM。

## 已知坑（血泪沉淀）

1. **postMessage 死循环**：给宿主窗口转发日志时，无 token 的消息会触发站点处理器内的
   "给 const 赋值" bug → 报错 → 再转发 → 死循环。对策：同源用函数直投、顶层页不给自己发、
   连续相同报错去重静默。
2. **hash 型 URL 的缓存参数必须放 hash 之前**：`/?_t=123#/pages/...`。
3. **登录态按端口源隔离**：换端口要在沙盒内重新登录。
4. **站点可能临时全站不可达**：直连/代理/第三方通道同时超时即站点侧故障，勿误判为代理问题。
5. **编辑工具保存偶发假报错**：SearchReplace 报 "save failed" 时先读文件确认实际落盘状态再决定是否重试。

## 运行方式

```powershell
$env:PORT=8793; node server.js
# 浏览器打开 http://localhost:8793/wb/workbench.html
```

大模型：AI 面板右上角 ⚙ → 填 Base URL / API Key → 「⬇ 拉取模型列表」→ 保存。
保存后 Key 由本地服务保管；旧版浏览器 Key 会自动迁移。未配 Key 时自动降级为内置规则引擎。
